import 'package:flutter/material.dart';
import 'package:get/get.dart';

class NewEvent extends StatelessWidget {
  const NewEvent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("New Event"));
  }
}
